import { Router } from 'express';

const router = Router();

// Placeholder for profile routes - to be implemented
router.get('/', (req, res) => {
  res.json({ message: 'Profile routes - coming soon' });
});

export default router;