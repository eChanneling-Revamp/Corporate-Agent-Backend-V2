import { Router } from 'express';

const router = Router();

// Placeholder for payment routes - to be implemented
router.get('/', (req, res) => {
  res.json({ message: 'Payment routes - coming soon' });
});

export default router;