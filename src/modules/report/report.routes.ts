import { Router } from 'express';

const router = Router();

// Placeholder for report routes - to be implemented
router.get('/', (req, res) => {
  res.json({ message: 'Report routes - coming soon' });
});

export default router;